import 'package:flutter/cupertino.dart';

class LearnPage extends StatelessWidget {
  const LearnPage({super.key});


  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("学习", style: TextStyle(fontSize: 24)));
  }
}